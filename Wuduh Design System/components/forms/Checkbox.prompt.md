Checkbox / single-select option. Use `round` for radio-style mutually-exclusive option lists on cards.

```jsx
<Checkbox label="Pre-revenue" />
<Checkbox round name="stage" label="Generating revenue" defaultChecked />
```

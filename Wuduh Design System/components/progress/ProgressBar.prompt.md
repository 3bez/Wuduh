Linear progress bar. Gold fill communicates overall study completion; teal for an in-section streak.

```jsx
<ProgressBar value={4} max={12} label="Your study" showCount />
<ProgressBar value={80} tone="teal" size="lg" />
```
